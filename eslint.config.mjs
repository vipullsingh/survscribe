/**
 * Root ESLint entry. The shared rules live in @survscribe/config so every package -- and
 * anything added later -- lints the same way.
 */
import config from "@survscribe/config/eslint";

export default config;
