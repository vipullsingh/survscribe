# React Native's Gradle and CocoaPods autolinking, and the @react-native/jest-preset,
# all assume a flat node_modules layout. pnpm's default symlinked store breaks them in
# ways that surface as opaque native build failures rather than resolution errors.
node-linker=hoisted
