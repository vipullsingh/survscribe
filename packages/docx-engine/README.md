# SurvScribe Shared Document Engine Contracts (`packages/docx-engine`)

TypeScript template schema definitions and parity contracts shared between the client-side React Native `.docx` draft generator and the server-side Go report compilation engine.
