/**
 * React Native 0.87 no longer ships a `jest-preset.js` inside the react-native package
 * itself; the preset moved to @react-native/jest-preset.
 */
module.exports = {
  preset: "@react-native/jest-preset",
};
